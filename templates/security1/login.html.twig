{% extends  'security1/layout.html.twig' %}
{% block body %}
<div class="wel">
<div class="container-fluid my-class">

        <h4> CONNEXION </h4>
        <form action="{{ path('login') }}" method="post">
         {# <div class="alert alert-success" role="alert">
            Identifiant = n°tél 13chiffres.
            </div>  #}
             {% if error %}
            <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}  
        <div class="form-group">
            <label for="username">Identifiant</label>
            <input type="text" style="background-color:powderblue" placeholder="Tel:13 chiffres" id="username" name="_username" value="{{ last_username }}" />
        </div>
         <div class="form-group">
            <label for="password">Mot  de  Pass</label>
            <input type="password" style="background-color:powderblue" id="password" name="_password" required="required" autocomplete="current-password" />
        </div> 
         {# <div class="form-group">     
            <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
            <label for="remember_me">{{ 'security.login.remember_me'|trans }}</label>
         </div>  #}
            <button type="submit" class="label label-success"><h8>Connexion!</h8> </button>
			{# <a href="{{ path('app_logout') }}"><p><h6><span class="label label-warning">Annuler</span></h6></p></a> #}
			<a href="{{ path('forgetpass') }}"><p><h6><span class="label label-info">Mot de passe oublié!</span></h6></p></a>
        </form>     
</div>
</div>
{% endblock %}